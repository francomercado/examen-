#include "stdafx.h"
#include <iostream>

using namespace std;
void main()
{
	float area,b,h;
	int op;
    cout<<"digite"<<endl;
	cout<<"1 opcion area del rectangulo"<<endl;
	cout<<"2 opcion area del triangulo"<<endl;
	cout<<"3 opcion no hacer nada"<<endl;
	cout<<"escrbir opcion"<<endl;
	cin>>op;
	if (op==1)
    {
	  cout<<"ingrese base"<<endl;
	  cin>>b;
	  cout<<"ingrese altura"<<endl;
	  cin>>h;
	  area= b*h;
	  cout<<"el area es"<<area;
    }
    if(op==2)
    {
      cout<<"ingrese base"<<endl;
      cin>>b;
      cout<<"ingrese altura"<<endl;
      cin>>h;
      area= b*h/2;
      cout<<"el area es"<<area;
   }
   if(op==3)
	  cout<<"gracias";
